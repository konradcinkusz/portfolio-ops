<!-- Generated by portfolio-ops. The workflow rewrites this page from the data files, so edit those instead. -->

# Risks and findings

[Overview](README.md) · [Products](products.md) · [Repositories](repositories.md) · **Risks** · [Decisions](decisions.md)

Generated by portfolio-ops for 2026-09-22 (UTC). These pages show the state of the data files and decide nothing: the rules run in validate, the weekly report and the gates.

## Risks

4 risks; 3 count as open and come first — open, or accepted past accepted_until.

| Risk | Scope | Severity | Applies to | State |
|---|---|---|---|---|
| The first sync after an update can crash the app (`alpha-crash`) | Alpha (`alpha`) | **critical** | store | **open** |
| Two offline edits can overwrite each other (`core-data-loss`) | Core (`core`) | **high** | all | **acceptance ended** 2026-09-01; counts as open |
| A dependency's licence may forbid resale (`alpha-licence`) | Alpha (`alpha`) | medium | store | **open** |
| One hosting account serves every product (`portfolio-hosting`) | the portfolio | low | all | closed |

## Findings

2 findings, the soonest to expire first; 1 expired — check an expired one again before it is used.

| Finding | Subject | Type | Checked on | Holds until | Used in |
|---|---|---|---|---|---|
| Syncs a change in under a second on a phone (`alpha-sync-claim`) — evidence: Timed on two phones over the office network | Alpha (`alpha`) | claim | 2026-06-01 | 2026-09-01, **expired** | website |
| No product in the store uses the name (`delta-name`) | Delta (`delta`) | name\_check | 2026-09-10 | 2026-12-09 | — |

## Copy-paste debt

Products that carry a copy of a kernel instead of using it as a package:

| Product | Kernel | Kernel state |
|---|---|---|
| Epsilon (`epsilon`) | Core (`core`) | extracted, 1 of 2 package consumers |
